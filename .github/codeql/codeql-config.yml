name: "CodeQL-Config"

queries:
  - uses: github/codeql/javascript-queries@main
  - uses: github/codeql/typescript-queries@main

paths:
  - src/
  - lib/
  - tests/

options:
  - name: "verbosity"
    value: "debug" # Increased verbosity for better debugging